# Secrets — never commit
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
venv/
.venv/
*.egg-info/

# Render / server lock files
data/.server-*.lock

# Generated backups / temp data snapshots
data/*.backup*.json
data/peopleops-data-month.json

# OS
.DS_Store
Thumbs.db

# Local env files
env
env (*)

# Work location cache: per-employee attendance, built by the server at runtime. Never commit (repo is public).
data/worklocation/
